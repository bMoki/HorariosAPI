spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/ProjectIFRS"
    username: "postgres"
    password: "1234"

  jpa:
    hibernate:
      ddl-auto: "create-drop"

    show-sql: "true"

    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
        format_sql: "true"